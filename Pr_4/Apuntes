PR4--------------------------------
Ej1)
	Usaremos 2 flujos:
		1º -> Publique en MQTT (Usando el broker Mosquitto) un nº aleatorio entre 10 y 100 ~ 10s
		2º -> Reciba el nº a través de MQTT. Si es par -> acumula el sumatorio -> Local al nodo (alcance variables node-red). 
	 	      Sea impar o no, depuración (mostrar) el número y el acumulador-

EJ2)
	SERVIDOR. Tenemos un ENDPOINT POST. Solo en 1 flujo:
		body -> {"str": "Hola"}. Le da la vuelta a la cadena. Develve al cliente {"str_reversed":"aloH"}. Podemos probarlo con Postman o Curl
		
EJ3)
	Bot de Telegram.
	
	Se pide que el bot tenga 3 acciones (a nuestro juicio, se puntúa la originalidad)
	
	Se envía un flujo y un vídeo de max 5mins contando lo que hace nuestro bot.
